const queryKeys = {
  projects: {
    listActive: 'projects',
    listArchived: 'projects/archived',
    admin: 'projects-admin'
  },
  files: {
    markups: 'markups'
  }
}

export default queryKeys
